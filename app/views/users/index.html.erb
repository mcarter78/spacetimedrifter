<div id="main-content">
  <h1>Current Voyagemenauts:</h1>

  <ul>
    <% @users.each do |user| %>
      <li><%= image_tag("#{user.avatar_url}", alt: "#{user.username} avatar") %><%= user.username %></li>
    <% end %>
  </ul>
</div>
