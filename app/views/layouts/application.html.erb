<!DOCTYPE html>
<html>
<head>
  <title>SpaceTimeDrifter</title>
  <link href='https://fonts.googleapis.com/css?family=Orbitron:400,700,900' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <%= link_to "SpaceTimeDrifter", root_path, id: "hero" %>
    <% if @current_user %>
      <%= link_to "Log Out", sessions_path, method: :delete, id: "logout-button" %>
      <%= link_to "#{@current_user.username}'s Profile    ", @current_user, method: :get, id: "profile-button" %>
    <% else %>
      <%= link_to "Log In", sign_in_path, method: :get, id: "login-button" %>
    <% end %>
  </header>

  <%= yield %>

  <footer>
    <% if @current_user %>
      <% if current_page? root_path %>
        <%= nil %>
        <% else %>
          <div id="footer-buttons">
            <%= button_to "Drifters", users_path, method: :get, class: "footer-button" %>
            <%= button_to "Destinations", destinations_path, method: :get, class: "footer-button" %>
          </div>
      <% end %>
    <% end %>
  </footer>
</body>
</html>
